const { PrismaClient } = require("./server/generated/prisma/index.js"); const prisma = new PrismaClient(); prisma.user.create({ data: { name: "Directorate Admin Central", fullName: "Directorate Admin Central", email: "admin.central@emaap.gov.in", mobile: "9999999999", role: "ADMIN", registrationRole: "ADMIN", jurisdiction_district: "Central", jurisdiction_state: "Central", passwordHash: "dummyhash", emailVerified: true, mobileVerified: true, isActive: true } }).then(u => { console.log("Admin ID:", u.user_id); return prisma.$disconnect(); }).catch(e => { console.error(e); process.exit(1); })
